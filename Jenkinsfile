import groovy.transform.ToString;
import groovy.transform.EqualsAndHashCode;
@EqualsAndHashCode
@ToString

pipeline {
    agent any
    
    stages {
        stage('Build') { 
            steps {
                sh 'hello' 
            }
        }
    }
}